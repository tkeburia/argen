package com.pagofx.feature.{{ .ModuleNameLowerCase }}

import org.kodein.di.Kodein

private const val MODULE_PREFIX = "{{ .ModuleNameLowerCase }}Feature"

val {{ .ModuleNameLowerCase }}FeatureModule = Kodein.Module("${MODULE_PREFIX}Module") {
    import(presentationModule)
}

private val presentationModule = Kodein.Module("${MODULE_PREFIX}PresentationModule") {
}
